package com.example.software_engineeringproject;
/*************************************************************************************
 * 用于处理发送与接受的的信息
 * 所有信息生成为.json格式，方便后续的解析工作
 * 信息的格式如下(可以选择填入，未填入的部分设置为缺省值null)：
 * Create_msg_for_client(
 *      String type，        指令类型
 *      String id,           用户账号
 *      String target_id，   传送目标的账号
 *      String data          要传输的数据
 * )
 *************************************************************************************/
public class depends {
    public String Create_msg_for_client() {
        String s="";
        return s;
    }

    public String Get_info_from_message(String message,String item){
        String s="";
        return s;
    }
}
